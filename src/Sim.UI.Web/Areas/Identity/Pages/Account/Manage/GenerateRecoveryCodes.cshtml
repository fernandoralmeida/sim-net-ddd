@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Gerar códigos de recuperação de autenticação de dois fatores (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}
<h5>@ViewData["Title"]</h5>
<partial name="_StatusMessage" for="StatusMessage" />
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Coloque esses códigos em um lugar seguro.</strong>
    </p>
    <p>
        Se você perder seu dispositivo e não tiver os códigos de recuperação, perderá o acesso à sua conta.
    </p>
    <p>
        A geração de novos códigos de recuperação não altera as chaves usadas nos aplicativos autenticadores. Se você deseja mudar a chave
        usado em um aplicativo autenticador, você deve <a asp-page="./ResetAuthenticator">redefina as chaves do autenticador.</a>
    </p>
</div>
<div>
    <form method="post" class="row g-3">
        <button class="btn btn-danger" type="submit">Gerar Códigos de Recuperação</button>
    </form>
</div>